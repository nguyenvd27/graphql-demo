"""
Root Query
"""
type Query

"""
Root Mutation
"""
type Mutation

directive @needAuthentication on FIELD_DEFINITION
directive @needRole(role: Role!) on FIELD_DEFINITION

enum Role {
  ADMIN
  USER
}