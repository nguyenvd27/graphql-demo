extend type Query {
  todos: [Todo!]!
  todo(id: ID!): Todo!
}

extend type Mutation {
  createTodo(input: NewTodo!): Todo!
}

input NewTodo {
  text: String!
  userId: String!
}

type Todo {
  id: ID!
  text: String!
  done: Boolean!
  user: User!
}